namespace Beck.Docs;

/// <summary>The site's one deployed origin — GitHub Pages, sub-path = repo name. Feeds
/// Pennington's <c>CanonicalBaseUrl</c> (canonical links, sitemap, og:image URLs) and the
/// absolute card URLs <c>SocialMeta</c> emits for the Razor pages.</summary>
public static class SiteInfo
{
    public const string CanonicalBaseUrl = "https://usepennington.github.io/beck";
}
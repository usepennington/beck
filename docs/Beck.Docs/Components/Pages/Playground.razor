@page "/playground"
@using Beck.Docs.Client

<PageTitle>Playground — Beck</PageTitle>

@* Static-SSR route so Pennington's crawler emits /playground into the static export; the
   interactive UI is a WebAssembly island (Beck.Docs.Client) that renders diagrams in the
   browser via the pure-C# engine. Its own @@rendermode drives the interactivity. *@
<PlaygroundIsland />

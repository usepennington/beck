@* Markdown-embedded icon reference: `<IconGallery />`. The icon set lives in the engine
   (window.Beck.icons); site.js fills this container client-side from that registry, so the page
   can never drift from the shipped bundle. Aliases that share a glyph collapse onto one card. *@

<div class="not-prose my-7" data-beck-icon-gallery>
    <p class="font-mono text-sm text-base-500 dark:text-base-400">Loading icons…</p>
</div>
